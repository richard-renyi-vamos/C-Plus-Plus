#include<iostream>
#include<math.h>
using namespace std;

int main(){
  float number, root;
  cout << "Enter number, I will calculate it's square root: ";
  cin >> number;
  root = sqrt(number);
  cout << "Square root of " << number << " is " << root;
  return 0;
}
